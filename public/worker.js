// const keypage = require("../keypage")

onmessage = function(data){
    // const page = await Request.get(`/page.json`)
    // pages = JSON.parse(page.text)
    console.log("pong")
    // const keypage = await Request.get(`/keypage.json`)
    // const pageToCat = JSON.parse(keypage)//.text)
    postMessage(`worker:`)
}
